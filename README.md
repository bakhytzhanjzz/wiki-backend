# Wiki Backend - Inventory & POS System

Backend приложение для системы управления инвентарем и POS (Point of Sale) с поддержкой multi-tenancy архитектуры.

## 📋 Описание

Wiki Backend - это RESTful API сервер, построенный на Spring Boot, который предоставляет функциональность для управления:
- Товарами и категориями
- Продажами и возвратами
- Складом и инвентаризацией
- Клиентами и долгами
- Поставщиками и заказами
- Маркетингом (акции, промокоды, подарочные карты, SMS-рассылки)
- Аналитикой и отчетностью
- Пользователями и ролями

## 🛠 Технологии

- **Java 21** - Язык программирования
- **Spring Boot 4.0.0** - Фреймворк
- **Spring Security** - Безопасность и аутентификация
- **Spring Data JPA** - Работа с базой данных
- **PostgreSQL** - Реляционная база данных
- **JWT (JSON Web Tokens)** - Аутентификация через токены
- **Lombok** - Упрощение boilerplate кода
- **Spring AOP** - Аспектно-ориентированное программирование для аудита
- **Maven** - Управление зависимостями и сборка
- **Docker** - Контейнеризация

## 🏗 Архитектура

### Multi-Tenancy

Система поддерживает multi-tenant архитектуру:
- Каждая сущность наследуется от `BaseTenantEntity`
- Автоматическая изоляция данных по `tenantId`
- Генерация `tenantId` из названия компании через `TenantIdGenerator`
- Фильтрация всех запросов по `tenantId` на уровне JPA

### Безопасность

- **JWT аутентификация** с access и refresh токенами
- **Роли пользователей**: `OWNER`, `ADMIN`, `SELLER`
- **Role-based access control (RBAC)** через `@PreAuthorize`
- **BCrypt** для хеширования паролей
- **Stateless** сессии (без сохранения состояния на сервере)

### Аудит

- Автоматическое логирование операций через AOP
- Аннотация `@AuditLoggable` для отслеживания изменений
- Хранение истории изменений в `AuditLog`

## 📦 Структура проекта

```
wiki-backend/
├── src/
│   ├── main/
│   │   ├── java/com/kz/wiki/
│   │   │   ├── annotation/          # Кастомные аннотации
│   │   │   ├── aspect/              # AOP аспекты
│   │   │   ├── config/              # Конфигурационные классы
│   │   │   ├── controller/          # REST контроллеры (25 контроллеров)
│   │   │   ├── dto/                 # Data Transfer Objects
│   │   │   │   ├── request/         # DTO для запросов
│   │   │   │   └── response/        # DTO для ответов
│   │   │   ├── entity/              # JPA сущности (40+ сущностей)
│   │   │   ├── exception/           # Обработка исключений
│   │   │   ├── repository/          # Spring Data JPA репозитории
│   │   │   ├── security/           # JWT и безопасность
│   │   │   ├── service/             # Бизнес-логика
│   │   │   │   └── impl/            # Реализации сервисов
│   │   │   ├── util/                # Утилиты
│   │   │   ├── validation/          # Кастомные валидаторы
│   │   │   └── WikiApplication.java
│   │   └── resources/
│   │       ├── application.properties
│   │       └── messages*.properties # Интернационализация
│   └── test/                        # Тесты
├── pom.xml                          # Maven конфигурация
├── Dockerfile                       # Docker образ
└── README.md                        # Этот файл
```

## 🚀 Быстрый старт

### Требования

- Java 21 или выше
- Maven 3.9+
- PostgreSQL 12+ (или используйте предоставленную базу на Render.com)
- (Опционально) Docker для контейнеризации

### Локальная установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/bakhytzhanjzz/wiki-backend.git
cd wiki-backend
```

2. **Настройте базу данных:**
   
   Отредактируйте `src/main/resources/application.properties`:
```properties
spring.datasource.url=jdbc:postgresql://your-db-host:5432/your-database
spring.datasource.username=your-username
spring.datasource.password=your-password
```

3. **Соберите проект:**
```bash
mvn clean install
```

4. **Запустите приложение:**
```bash
mvn spring-boot:run
```

Или используйте Maven wrapper:
```bash
./mvnw spring-boot:run
```

Приложение будет доступно по адресу: `http://localhost:8081`

### Docker

1. **Соберите Docker образ:**
```bash
docker build -t wiki-backend .
```

2. **Запустите контейнер:**
```bash
docker run -p 8081:8081 \
  -e SPRING_DATASOURCE_URL=jdbc:postgresql://host:5432/db \
  -e SPRING_DATASOURCE_USERNAME=user \
  -e SPRING_DATASOURCE_PASSWORD=pass \
  -e PORT=8081 \
  wiki-backend
```

## ⚙️ Конфигурация

### Основные настройки (application.properties)

```properties
# База данных
spring.datasource.url=jdbc:postgresql://host:5432/database
spring.datasource.username=username
spring.datasource.password=password

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Сервер
server.port=${PORT:8081}

# JWT
jwt.secret=your-256-bit-secret-key-must-be-at-least-32-characters-long
jwt.expiration=86400000          # 24 часа
jwt.refresh-expiration=604800000 # 7 дней

# CORS
cors.allowed.origins=*
```

### Переменные окружения

- `PORT` - Порт сервера (по умолчанию 8081)
- `SPRING_DATASOURCE_URL` - URL базы данных
- `SPRING_DATASOURCE_USERNAME` - Имя пользователя БД
- `SPRING_DATASOURCE_PASSWORD` - Пароль БД
- `JWT_SECRET` - Секретный ключ для JWT (если не указан, используется из properties)

## 📡 API Документация

Полная документация API доступна в файле [API_DOCUMENTATION.md](./API_DOCUMENTATION.md)

### Базовый URL
```
http://localhost:8081/api
```

### Аутентификация

Большинство эндпойнтов требуют JWT токен в заголовке:
```
Authorization: Bearer <access_token>
```

Публичные эндпойнты (без аутентификации):
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/refresh` - Обновление токена
- `GET /actuator/health` - Health check

### Основные модули API

| Модуль | Базовый путь | Описание |
|--------|--------------|----------|
| **Authentication** | `/api/auth` | Регистрация, вход, обновление токенов |
| **Products** | `/api/products` | Управление товарами |
| **Categories** | `/api/categories` | Управление категориями |
| **Sales** | `/api/sales` | Продажи, возвраты, обмены |
| **Stock** | `/api/stock` | Управление складом |
| **Clients** | `/api/clients` | Управление клиентами |
| **Client Debts** | `/api/client-debts` | Долги клиентов |
| **Client Groups** | `/api/client-groups` | Группы клиентов |
| **Client Tags** | `/api/client-tags` | Теги клиентов |
| **Suppliers** | `/api/suppliers` | Управление поставщиками |
| **Orders** | `/api/orders` | Заказы поставщикам |
| **Imports** | `/api/imports` | Импорт товаров |
| **Inventory** | `/api/inventory` | Инвентаризация |
| **Transfers** | `/api/transfers` | Трансферы между складами |
| **Repricing** | `/api/repricing` | Переоценка товаров |
| **Write-offs** | `/api/write-offs` | Списание товаров |
| **Stores** | `/api/stores` | Управление магазинами |
| **Users** | `/api/users` | Управление пользователями |
| **Analytics** | `/api/analytics` | Аналитика и статистика |
| **Discounts** | `/api/discounts` | Скидки |
| **Gift Cards** | `/api/gift-cards` | Подарочные карты |
| **Loyalty Program** | `/api/loyalty-program` | Программа лояльности |
| **Payment Methods** | `/api/payment-methods` | Способы оплаты |

### Формат ответов

Все ответы обернуты в `ApiResponse<T>`:

```json
{
  "success": true,
  "message": "Опциональное сообщение",
  "data": { ... },
  "timestamp": 1234567890
}
```

Ошибки:
```json
{
  "success": false,
  "message": "Описание ошибки",
  "data": null,
  "timestamp": 1234567890
}
```

## 🔐 Безопасность

### Роли пользователей

- **OWNER** - Полный доступ ко всем операциям
- **ADMIN** - Административный доступ
- **SELLER** - Доступ к продажам и базовым операциям

### Endpoints по ролям

Большинство эндпойнтов защищены через `@PreAuthorize`:
- `@PreAuthorize("hasRole('OWNER')")` - Только владелец
- `@PreAuthorize("hasAnyRole('OWNER', 'ADMIN')")` - Владелец или админ
- `@PreAuthorize("hasAnyRole('OWNER', 'ADMIN', 'SELLER')")` - Все роли

### JWT Токены

- **Access Token** - Короткоживущий (24 часа), используется для API запросов
- **Refresh Token** - Долгоживущий (7 дней), используется для обновления access token

## 📊 Основные сущности

### Продукты и склад
- `Product` - Товары
- `Category` - Категории товаров
- `StockTransaction` - Транзакции склада
- `Import` - Импорт товаров
- `Inventory` - Инвентаризация
- `Transfer` - Трансферы между складами
- `Repricing` - Переоценка
- `WriteOff` - Списание

### Продажи
- `Sale` - Продажи
- `SaleItem` - Позиции продажи
- `Order` - Заказы поставщикам
- `OrderItem` - Позиции заказа

### Клиенты
- `Customer` / `Client` - Клиенты
- `CustomerDebt` / `ClientDebt` - Долги клиентов
- `ClientGroup` - Группы клиентов
- `ClientTag` - Теги клиентов
- `LoyaltyProgram` - Программа лояльности

### Маркетинг
- `Discount` - Скидки
- `GiftCard` - Подарочные карты
- `Promotion` - Акции (через Discount)

### Организация
- `Company` - Компания (tenant)
- `Store` - Магазины
- `Supplier` - Поставщики
- `User` - Пользователи
- `PaymentMethod` - Способы оплаты

## 🧪 Тестирование

### Запуск тестов
```bash
mvn test
```

### Health Check
```bash
curl http://localhost:8081/actuator/health
```

## 🐳 Docker

### Сборка образа
```bash
docker build -t wiki-backend:latest .
```

### Запуск контейнера
```bash
docker run -d \
  --name wiki-backend \
  -p 8081:8081 \
  -e SPRING_DATASOURCE_URL=jdbc:postgresql://host:5432/db \
  -e SPRING_DATASOURCE_USERNAME=user \
  -e SPRING_DATASOURCE_PASSWORD=pass \
  wiki-backend:latest
```

### Multi-stage build

Dockerfile использует multi-stage build:
1. **Stage 1 (build)**: Maven сборка приложения
2. **Stage 2 (runtime)**: Легковесный JRE образ с приложением

## 🌐 Развертывание

### Render.com

Проект настроен для развертывания на Render.com:
- Использует переменную окружения `PORT`
- Поддерживает PostgreSQL из Render
- Health check через `/actuator/health`

### Другие платформы

Приложение может быть развернуто на любой платформе, поддерживающей Java 21:
- Heroku
- AWS Elastic Beanstalk
- Google Cloud Run
- Azure App Service
- Любой VPS с Java 21

## 📝 Интернационализация

Система поддерживает несколько языков:
- `messages.properties` - По умолчанию (английский)
- `messages_ru.properties` - Русский
- `messages_kz.properties` - Казахский

## 🔍 Мониторинг

### Actuator Endpoints

- `/actuator/health` - Статус здоровья приложения
- Другие endpoints могут быть настроены через `application.properties`

## 📚 Дополнительная документация

- [API_DOCUMENTATION.md](./API_DOCUMENTATION.md) - Полная документация API
- [IMPLEMENTATION_STATUS.md](./IMPLEMENTATION_STATUS.md) - Статус реализации модулей
- [Wiki_API_Collection.postman_collection.json](./Wiki_API_Collection.postman_collection.json) - Postman коллекция для тестирования

## 🤝 Разработка

### Структура кода

- **Controllers** - REST API endpoints, валидация входных данных
- **Services** - Бизнес-логика, транзакции
- **Repositories** - Доступ к данным через Spring Data JPA
- **Entities** - JPA сущности, маппинг на таблицы БД
- **DTOs** - Data Transfer Objects для запросов/ответов
- **Security** - JWT аутентификация и авторизация
- **Config** - Конфигурационные классы Spring

### Паттерны

- **Repository Pattern** - Абстракция доступа к данным
- **Service Layer Pattern** - Разделение бизнес-логики
- **DTO Pattern** - Изоляция внутренней модели от API
- **Multi-Tenancy Pattern** - Изоляция данных по tenant

## ⚠️ Важные замечания

1. **Multi-Tenancy**: Все сущности автоматически фильтруются по `tenantId`
2. **Безопасность**: Пароли хешируются через BCrypt
3. **JWT**: Секретный ключ должен быть минимум 32 символа
4. **База данных**: Используется `ddl-auto=update` для автоматической миграции схемы
5. **CORS**: В продакшене настройте `cors.allowed.origins` на конкретные домены
6. **Redis**: Отключен по умолчанию (можно включить для кеширования)

## 📄 Лицензия

Проект находится в разработке.

## 👥 Авторы

Разработано для системы управления инвентарем и POS.

---

**Версия:** 0.0.1-SNAPSHOT  
**Последнее обновление:** 2024

